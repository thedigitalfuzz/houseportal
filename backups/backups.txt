<script> function scrollToBottom() { const container = document.getElementById('chatContainer'); if (container) { container.scrollTop = container.scrollHeight; } } document.addEventListener("livewire:update", () => { scrollToBottom(); }); window.addEventListener("load", () => { scrollToBottom(); }); </script>
